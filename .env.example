# =====================================================
# PEDAGOGI.AI - ENVIRONMENT VARIABLES
# =====================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# -----------------------------------------------------
# SUPABASE (Required)
# -----------------------------------------------------
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://tjsvfzhqugyegbhnbgwk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_placeholder_key

# Service Role Key (Server-side only, NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=sb_secret_placeholder_key

# -----------------------------------------------------
# DATABASE ENCRYPTION (Required for Student Privacy)
# -----------------------------------------------------
# This key is used to encrypt student PII
# Generate a strong key: openssl rand -base64 32
# Set this in Supabase: ALTER DATABASE postgres SET app.settings.encryption_key = 'your-key';
APP_ENCRYPTION_KEY=your_generated_base64_encryption_key_placeholder

# -----------------------------------------------------
# AI SERVICES (Optional - for production OCR)
# -----------------------------------------------------
# OpenAI API Key (for GPT-4 Vision OCR)
OPENAI_API_KEY=sk-your-openai-key

# Anthropic API Key (alternative to OpenAI)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Gemini API Key (principal)
GEMINI_API_KEY=sk-gemini-your-gemini-key

# -----------------------------------------------------
# EMAIL (Optional - for notifications)
# -----------------------------------------------------
# Resend API Key: https://resend.com/api-keys
RESEND_API_KEY=re_123456789_placeholder

# -----------------------------------------------------
# WEB PUSH (Optional - for push notifications)
# -----------------------------------------------------
# Generate VAPID keys: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key_placeholder
VAPID_PRIVATE_KEY=your_vapid_private_key_placeholder
VAPID_SUBJECT=mailto:contato@inovacode.dev

# -----------------------------------------------------
# EDGE FUNCTION CONFIGURATION (Local Development)
# -----------------------------------------------------
# URL for the Edge Function (set in database settings)
EDGE_FUNCTION_URL=http://localhost:54321/functions/v1

# -----------------------------------------------------
# STRIPE (Required for Subscriptions)
# -----------------------------------------------------
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Product IDs (Create these in Stripe Dashboard)
STRIPE_PRICE_ID_PRO=price_...

# -----------------------------------------------------
# NEXT.JS (Optional)
# -----------------------------------------------------
# Set to 'production' for production builds
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:9901
